package application;

import javafx.event.ActionEvent;

public class UserHallWindow extends HallWindow {

	public UserHallWindow(Halls hall) {
		super(hall);
	}
	@Override
	public void handle(ActionEvent event) {
		super.handle(event);
	}
}
